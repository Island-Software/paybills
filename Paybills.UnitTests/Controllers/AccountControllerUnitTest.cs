using FluentAssertions;
using Moq;
using Paybills.API.Controllers;
using Paybills.API.DTOs;
using Paybills.API.Interfaces;

namespace Paybills.UnitTests.Controllers;

public class AccountControllerUnitTest
{
    // [Fact]
    // public async void Register_OnSuccess_ReturnsStatusCode200()
    // {
        // var mockUserRepository = new Mock<IUserRepository>();
        // var mockTokenService = new Mock<ITokenService>();
        // var accountController = new AccountController(mockUserRepository.Object, mockTokenService.Object);
        
        // var result = await accountController.Login(new Mock<LoginDto>().Object);

        // result.Result.Should().NotBeNull();
    // }
}    